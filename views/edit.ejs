<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
</head>
<body>
    <div class="bg-zinc-900 h-screen w-full p-10">
        <div class="flex gap-6 mb-10">
        <h1 class="text-4xl text-white">Edit Post</h1>
        <a href="/profile" class="bg-clip-text text-transparent text-2xl bg-gradient-to-r from-purple-500 to-red-400">View Profile</a>
        <a href="/post/read/<%= user._id %>" class="bg-clip-text text-transparent text-2xl bg-gradient-to-r from-cyan-500 to-blue-400">View Posts</a>
        </div>
        <div class="">
        <form action="/post/update/<%= post._id %>" method="POST" class="flex justify-evenly">
            
            <input type="text" name="title" placeholder="Edit Title" class="bg-zinc-700 text-white px-5 py-2 rounded-md">
            <textarea name="detail" placeholder="Edit Description" class="bg-zinc-700 text-white px-5 py-2 rounded-md resize-none"></textarea>
            
            <button type="submit" class="bg-gradient-to-r from-purple-500 to-blue-400 text-2xl text-white py-2 px-4 rounded-full hover:bg-blue-600" >
                <i class="ri-newspaper-line"></i>
            </button>
        </form>
        </div>

    </div>
    
</body>
</html>